# 体验优化计划（当前阶段 · 自用打磨）

- 基线版本：v0.1.70
- 定位：自用 / 自娱阶段，**不发布**；发布相关另见 `TODO-RELEASE.md`（准备发布时再启动）
- 生效方式：纯前端改动 **Ctrl+F5**；改 `server.py` 需重启并刷新；插件改动需重启 opencode
- 验证：`tools\test.bat`（`node --test` + `python -m unittest`）；改 JS 后先 `node --check`
- 约定：本地开发**不使用 git**

## P0（已完成 v0.1.53）
1. ~~**发送失败不再丢内容**~~：`send()` 改为 `postPrompt` 成功后再清空（失败回填并提示「内容已保留」）。见 `CHANGELOG.md` v0.1.53。
2. ~~**统一弹窗**~~：新增 `#confirmMask` / `confirmDialog()`，17 处 `alert/confirm` 清零；校验与失败走 `showToast(..., true)`。
3. ~~**错误附修复入口**~~：认证类错误（401/403、API key 无效等）气泡内加「去服务商重连」按钮。

## P1（全部完成）
4. ~~**跟随系统主题**~~（v0.1.54）：外观改为**独立三段选项** 亮色/暗色/跟随系统；`matchMedia` 监听系统偏好，仅 `auto` 生效；与主题色互不影响；补 `color-scheme`。
5. ~~**桌面通知**~~（v0.1.55）：回复完成 / `permission.asked` / `question.asked` 弹 Notification；仅「已开启 + 已授权 + 窗口失焦」时触发；设置独立开关。
6. ~~**数据导出 / 导入**~~（v0.1.56）：服务端 `/_profile` 落盘 `~/.config/opencode-chat/profile.json`，前端每 4s 自动保存 + 启动恢复；设置内提供导出 / 导入 JSON。另加「按助手清理记录」。
7. ~~**响应式 / 窄屏适配**~~：**不做**（实测窄屏无问题，用户确认）。改为在 v0.1.57 做「快捷键 + 触屏/键盘可用性」。
8. ~~**顶栏模型选择可搜索**~~（v0.1.58）：自定义 `#modelPick` 浮层，支持按提供商 / 模型名搜索，提供商可折叠（`oc_model_collapsed` 持久化），搜索时忽略折叠。

## P2（进行中）
9. ~~**会话列表增强**~~（v0.1.61/v0.1.62）：时间标签 + 按天分组、跨助手搜索（全量会话 + 助手徽标）、删除后恢复（本地「最近删除」回收站：撤销 / 恢复 / 彻底删除 / 清空）；确认弹窗置顶修复。
10. ~~**可访问性**~~（v0.1.57）：`title` → `aria-label` 自动补齐；树行/会话行 `tabIndex` + Enter/Space 激活 + `:focus-within` 展开操作；触屏常显行内操作。
11. ~~**快捷键扩展**~~（v0.1.57）：Ctrl+Enter 发送、Alt+N 新会话、Ctrl+, 设置、Esc（无弹窗时）中止生成；Ctrl+F 保留。
12. ~~**多图裁剪批量化**~~（v0.1.59）：裁剪器改队列模式（第 N/M 张），支持用原图 / 全部用原图 / 应用到其余 / 上一张；单张流程不变。
13. **对话托管体验**：轮询 600ms / 超时 180s，思考块刷新即丢（`proxy.js`）；可评估 SSE 或进度 / 持久化。
14. ~~**设置内搜索**~~（v0.1.66）：设置弹窗顶部搜索框，按关键词过滤设置项（多词「与」匹配），搜索时自动展开高级设置；含空结果提示。

## 新增需求（已完成）
- ~~**渲染质量滑块 + 背景升级 + 高质量动效**~~（v0.1.70）：三档渲染质量（性能优先/标准/高质量，旧低性能模式并入）；背景支持本地图片 / 本地视频（IndexedDB）/ 极光 / 星野 / 粒子 / 流光；高质量档含立体毛玻璃与各种动效。见 `CHANGELOG.md` v0.1.70。
- ~~**工具说明 / 新手易用性**~~（v0.1.68）：`TOOL_INFO` 为每个工具配中文名 + 一句话说明；助手→工具勾选与对话内工具卡片均显示中文名与说明；助手表单补充字段提示。见 `CHANGELOG.md` v0.1.68。
- ~~**低性能模式**~~（v0.1.67）：设置项开关，打包「长会话按需渲染（最近 40 条 + 加载更早）+ 关闭毛玻璃 + 关闭动画过渡 + 合并流式刷新」；内联提醒 + 状态 + Toast，切换即重渲染当前会话。见 `CHANGELOG.md` v0.1.67。
- ~~**设置内搜索**~~（v0.1.66）：设置弹窗顶部搜索框，按关键词过滤设置项（多词「与」匹配），搜索时自动展开高级设置；含空结果提示。
- ~~**消息翻译**~~（v0.1.60）：低cost模型 + 自定义提示词 / 思考强度（高级设置，可重置）；临时会话调用，不进当前对话。
- ~~**OCR 支持**~~（v0.1.63–v0.1.65）：设置内手动指定 OCR 模型（优先推荐专用模型）；工具栏 / 附件图片「OCR」→ 批量识别面板（结果可编辑 / 复制 / 插入）；助手级「自动 OCR」开关，开启后发送图片先转文字、图片不发模型，但图片经 IndexedDB 在对话中本地保留显示；自动 OCR 期间有进度提示与动效。见 `CHANGELOG.md` v0.1.63~v0.1.65。

## 备注
- 已评估放弃（见 `TODO.md`）：主线 SSE 改造、esbuild 模块化、热路径收敛、长会话按需渲染。
- 完成后把条目移入 `TODO.md`「已完成」并更新本节。
